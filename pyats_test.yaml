- hosts: N5KDC02_VLC
  connection: network_cli
  gather_facts: no
  roles:
    - ansible-pyats
    
  tasks:
    - pyats_parse_command:
        command: show interface port-channel3
      register: dc2
      when: "'N5KDC02_VLC' in group_names"



    - debug:
        var: output.structured

    # - copy:
    #     content: 

    # - name: print record 