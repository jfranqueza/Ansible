- hosts: lab
  connection: network_cli
  gather_facts: no
  roles:
    - ansible-pyats


  tasks:
    - include_role:
        name: ansible-pyats
        tasks_from: snapshot_command
      vars:
        command: show interfaces
        file: "interfaces.json"  #{{ inventory_hostname }}_