- name: TEST DE VLANS
  hosts: "{{ sw_name }}"
  gather_facts: no
  
  tasks:
    - name: create VLANS
      nxos_vlans:
        config:
          - vlan_id: "{{ item.id }}"
            name: "{{ item.name }}"
      loop: "{{vlans}}"


