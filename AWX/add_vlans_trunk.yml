- name: TEST DE VLANS
  hosts: "{{ sw_name }}"
  gather_facts: no


  tasks:
    - name: ADD VLAN TO TRUNK
      nxos_l2_interfaces:
        config:
          - name: "{{ item.intf }}"
            trunk: 
              allowed_vlans: "{{ item.vlans_in }}"
      loop: "{{ interfcs }}"